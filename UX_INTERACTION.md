# 我的小账本 UX 交互文档

> 文档版本：v1.0  
> 对应数据结构：schema v5  
> 审查日期：2026-07-30  
> 审查范围：`docs/index.html`、`docs/styles.css`、`docs/js/app.js`、`docs/js/model.js`、`docs/js/storage.js`、`README.md`

## 1．文档目的

本文档记录当前主账本已经实现的 UX 结构、核心任务流、页面状态、反馈与恢复规则，并作为后续交互调整的基线。

本文档同时包含一轮基于源码和 390×844 手机视口的 UX 审查。审查结论分为：

- “当前实现”：代码已经存在并经过本轮核对的行为。
- “问题”：当前实现可能导致误解、误操作、操作成本或可访问性缺陷。
- “建议”：尚未开发，不代表当前产品已经具备。

`README.md` 继续承担用户使用说明；本文档面向产品设计、交互评审和开发验收。

## 2．产品目标与交互原则

### 2.1 产品目标

主账本是一个手机端、本地优先的家庭支出记录与财务决策工具。它不以“记录越细越好”为目标，而是帮助使用者回答：

1. 钱主要花到了哪里。
2. 哪些支出是生活基础，哪些支出可以主动调整。
3. 本月计划是否正在偏离。
4. 下一阶段大致需要准备多少钱。
5. 月末应该保留什么结论，并采取什么行动。

### 2.2 当前边界

- 只新增支出，不新增逐笔收入。
- 没有账号、云同步、服务端或自动家庭合并。
- 每位家庭成员在自己的设备和浏览器中独立使用。
- 账目、家庭设置和规划数据保存在浏览器本地。
- JSON 完整备份是迁移和恢复数据的正式方式。
- 正式页面只接受完整备份 v5。

### 2.3 UX 原则

- 高频操作短：日常记账应在少量选择后显式保存。
- 决策维度稳定：用“固定必需、弹性必需、可选消费、专项突发”保持跨月可比。
- 内容维度自由：具体消费内容通过备注和正式专项表达。
- 本地数据可信：导入、删除、覆盖和异常恢复必须提供清晰反馈和恢复路径。
- 手机优先：主要触控区域不低于 44px，输入控件在手机端不低于 16px。
- 状态可辨认：已记录、无记录、已确认无支出和未来日期必须容易区分。
- 不输出伪精确结论：数据不足时不生成低可信度预测。

## 3．用户与使用情境

### 3.1 主要用户

- 家庭中的单个记账者。
- 在独立设备上记录自己或家庭支出的成员。
- 希望减少记账步骤，但仍需要月度预算、专项和目标判断的人。

### 3.2 高频情境

- 付款后立即记录一笔支出。
- 晚上通过日历补记当天支出。
- 搜索过去某笔消费。
- 查看本月支出结构和预算进度。
- 旅行、装修等事件结束后复盘花费。
- 月末记录一个结论和下月一件事。
- 更换设备或重要操作前导出完整备份。

## 4．信息架构

```text
我的小账本
├── 首页
│   ├── 周历／月历
│   ├── 当期支出摘要
│   ├── 日常预算
│   ├── 当前专项
│   ├── 支出结构
│   ├── 下月支出参考
│   ├── 本期观察
│   └── 更多分析
├── 明细
│   ├── 关键词搜索
│   ├── 日期筛选
│   ├── 年／月折叠列表
│   └── 复制／编辑／删除
├── 计划
│   ├── 预算
│   ├── 专项
│   ├── 目标
│   └── 月结
├── 快速记账
│   ├── 日期
│   ├── 获益方
│   ├── 支出属性
│   ├── 金额
│   ├── 备注／搜索
│   ├── 正式专项
│   ├── 快捷记录
│   └── 显式保存
└── 数据管理
    ├── 导出完整备份
    ├── 导入 v5 备份
    ├── 家庭成员
    ├── 旧版本升级门
    └── 数据救援
```

## 5．全局导航与页面框架

### 5.1 当前实现

- 页面顶部显示产品名称和不带云同步暗示的“数据”按钮。
- 底部固定导航为“首页、明细、计划”三个页面入口。
- 右下方 `56px` 圆形“＋”位于底部导航上方，并在三个一级页面中持续提供快速记账入口。
- 首页使用日历控制查看月份。
- 只有“计划—预算”和“计划—月结”显示顶部月份控制；明细、专项和目标不显示无效时间控件。
- 弹窗打开后锁定背景滚动，并限制焦点在弹窗中循环。
- 弹窗关闭后，焦点返回打开弹窗的原控件。

### 5.2 当前优点

- 一级入口数量少，用户容易建立页面心智模型。
- 记账入口全局可达，且没有被二级页面隐藏。
- 弹窗具备焦点管理和背景锁定，基础可访问性优于普通单页工具。

### 5.3 本轮优化

- 一级导航已经固定在手机底部，长页面滚动后仍可直接切换。
- “记一笔”与页面导航分离为圆形主操作，位置更接近右手拇指自然落点；页面底部留白保证最后一项内容可以滚动到按钮上方。
- Toast 位于圆形按钮上方；弹窗打开时按钮隐藏，避免和快速记账、撤销反馈重叠。
- 当前一级页面使用 `aria-current="page"`，计划二级入口使用 `aria-selected` 表达状态。

## 6．首页与日历

### 6.1 当前实现

首页默认显示包含锚点日期的一周：

- 左右箭头切换上一周和下一周。
- “••• 展开整月”切换为整月日历。
- 整月状态下，左右箭头切换上个月和下个月。
- 日期格包含“已记录、无记录、已确认无支出、未来日期”四种状态。
- 已记录日期显示当天支出合计。
- 点击无记录的今天或过去日期，直接打开对应日期的快速记账。
- 点击已有记录的日期，先打开当日支出详情。
- 点击未来日期只显示提示，不开放记账。

日历下方按当前所选月份或年份展示：

- 支出合计。
- 可支配金额。
- 同比或环比。
- 日常预算。
- 当前专项。
- 四种支出属性结构。
- 基础生活支出和保守可结余空间。
- 下月支出参考。
- 本期观察。
- 按需展开的年度趋势和家庭支出去向。

### 6.2 关键状态

| 日期状态 | 视觉含义 | 点击结果 |
| --- | --- | --- |
| 已记录 | 当天已有一笔或多笔支出 | 打开当日支出详情 |
| 无记录 | 过去或今天尚无支出记录 | 打开该日期快速记账 |
| 已确认无支出 | 用户主动确认当天没有支出 | 打开快速记账，可取消确认 |
| 未来日期 | 尚未到达 | 提示未来日期暂不记账 |

### 6.3 当前优点

- 日历同时承担补记入口和完整性检查，符合日常记账心智。
- 默认周视图控制了首页高度，整月按需展开。
- “无记录”与“已确认无支出”分离，避免把没有数据误判为零支出。
- 每日金额按整数分聚合后再转为元显示，口径正确。

### 6.4 历史月份语境

- 历史月份摘要使用“当月可支配”和“当月日常预算”。
- 历史月份不显示基于今天计算的“当前专项”和“下月支出参考”。
- 当前月份继续显示这两项，避免历史统计和今天状态混用。

## 7．快速记账

### 7.1 进入方式

- 点击页面右下方的圆形“＋”，默认记录今天。
- 点击首页无记录日期，记录所点日期。
- 在当日支出详情中点击“＋ 再记一笔”，继续记录该日期。
- 在已有账目菜单中选择“复制”，复用场景但生成新记录。
- 在已有账目菜单中选择“编辑”，修改原记录。

### 7.2 默认状态

快速记账以底部弹层呈现：

- 顶部固定区：标题、操作提示、无支出、关闭、日期、获益方、支出属性、金额、备注和专项入口。
- 中间滚动区：快捷记录。
- 底部固定区：保存按钮。

新增记录默认：

- 日期为入口决定的日期，使用只读标签。
- 获益方为本机默认成员。
- 未选择支出属性。
- 金额为空。
- 备注为空。
- 当前专项仅在日期落入专项周期时自动关联。
- 保存按钮禁用。

编辑记录时：

- 日期恢复为可修改控件。
- 原获益方、属性、金额、备注和专项被带入。
- 保存按钮文案为“保存修改”。

### 7.3 获益方

- 当前启用成员全部展示，不使用横向滑动。
- 1～4 个成员显示一行。
- 5～8 个成员按多列换行。
- 一次只能选择一个获益方。
- “共同”为保留成员。

### 7.4 支出属性

固定提供四种属性：

| 属性 | 用户判断 |
| --- | --- |
| 固定必需 | 稳定且短期难以减少 |
| 弹性必需 | 必须发生但金额可以优化 |
| 可选消费 | 可以取消、推迟或降低 |
| 专项突发 | 不进入普通月份基线 |

### 7.5 金额、备注与搜索

- 金额和备注同排，高度均为 52px。
- 金额最多保留两位小数，且必须大于 0。
- 备注最多 40 个字符。
- 保存时去除首尾空格并合并连续空格。
- 备注同时是快捷记录搜索框。
- 搜索按照完全匹配、前缀匹配和包含匹配排序。

### 7.6 快捷记录

快捷记录由历史账目实时生成，场景由以下字段共同决定：

- 支出属性。
- 获益方。
- 正式专项。
- 备注。

当前规则：

- 不复用历史金额和日期。
- 点击快捷记录只填充场景，不立即保存。
- 移动端点击快捷记录时，先阻止备注框失焦引发的布局切换；支出属性、获益方、专项和备注全部填充后，再退出搜索模式。
- 卡片右上角显示纯数字使用次数。
- 默认显示 6 项。
- 点击“展开更多”后显示所有可用历史场景，没有固定总数上限。
- 排序综合当前获益方、当前专项、使用次数和最近使用时间。
- 搜索时优先完全匹配，再显示前缀和包含匹配。
- 已停用成员和当前日期不适用的专项场景不显示。

### 7.7 备注搜索模式

聚焦备注后，页面进入压缩搜索状态：

- 收起标题说明、日期、完整获益方、完整支出属性和专项入口。
- 显示当前“获益方 · 支出属性 · 专项”摘要。
- 保留金额和备注输入框。
- 快捷记录在剩余区域独立滚动。
- 保存按钮保持在可视键盘上方。

页面通过 `visualViewport` 判断输入法状态：

- 可视高度相对基准减少至少 140px，视为输入法已经出现。
- 输入法出现后，可视高度差恢复到 80px 以内，视为输入法已经关闭。
- 关闭后备注输入框自动失焦，完整表单恢复。
- 草稿、搜索词和选择状态保留。

### 7.8 保存与撤销

- 金额有效且已经选择支出属性时，保存按钮启用。
- 新增与编辑统一显式保存。
- 新增成功后提供 5 秒撤销。
- 开始下一笔记录时，上一笔新增撤销提示立即收起。
- 删除记录后提供 12 秒撤销。

### 7.9 确认无支出

- 新增页面右上角显示“✓ 无支出”。
- 该日期已经存在支出时不显示。
- 空白表单可以直接确认。
- 已填写金额或备注时，第一次点击进入警告状态，第二次点击才会放弃草稿并确认。
- 确认和取消均提供 12 秒撤销。
- 后续新增该日支出时，自动取消无支出确认。

### 7.10 当前优点

- 显式保存使选择和提交边界清楚，降低快捷记录误触即保存的风险。
- 快捷记录不复用金额，避免历史价格污染新记录。
- 搜索、历史场景和标准化备注形成了同一条低成本路径。
- 键盘出现时压缩非必要信息，解决了搜索结果和保存按钮被遮挡的问题。
- 新增、删除和无支出均有与风险相匹配的恢复机制。

### 7.11 草稿与可访问状态

- 表单已经发生修改时，点击背景、关闭按钮或按 `Escape` 会先确认是否放弃。
- 获益方、四种支出属性和无支出状态使用 `aria-pressed` 表达选中状态。
- 快捷记录数量多时仍只提供展开全部。当前排序已经覆盖大多数场景，因此暂不增加“常用／最近”切换，避免重新引入复杂度。

## 8．明细

### 8.1 当前实现

- 搜索支持备注、专项名称、获益方和支出属性。
- 日期范围支持全部、本月、上月、今年和自定义。
- 结果按年份和月份折叠。
- 当前年份和月份默认展开。
- 搜索结果自动展开并显示笔数和合计。
- 每笔记录提供复制、编辑和删除。

### 8.2 时间定位

- 已删除不影响列表的顶部月份和“月／年”控件。
- 用户通过年／月折叠列表浏览时间，通过日期范围筛选精确限制结果。
- 日期范围不是“自定义”时，不显示无效的起止日期输入。

### 8.3 当前状态

- 搜索占位文案已经覆盖备注、获益方、专项和支出属性。
- 只有选择“自定义”日期范围时才显示起止日期输入。
- 年份和月份折叠按钮使用 `aria-expanded` 表达状态。

## 9．计划

### 9.1 二级导航

计划页包含：

- 预算。
- 专项。
- 目标。
- 月结。

### 9.2 预算

当前流程：

1. 通过顶部月份切换选择月份。
2. 填写可支配金额和日常预算，至少填写一项。
3. 可在空计划时复制上月计划。
4. 点击“保存月度计划”。
5. 页面计算计划后结余或缺口。

当前问题：

- 已设置的两项金额不能通过全部留空来清除，因为系统会提示“请至少填写一项”。
- 离开预算页时，尚未保存的输入会直接丢失。

### 9.3 专项

专项用于归集旅行、装修、节日、医疗、教育、搬家等完整事件。

当前流程：

- 新建专项。
- 选填预算和日期范围。
- 旅行专项填写参与人数。
- 查看历史同类专项参考。
- 设为或取消当前专项。
- 完成后保留历史并默认折叠。
- 查看专项明细和历史对比。

当前状态：

- 新建和编辑表单发生修改后，关闭前会确认是否放弃，避免误触丢失内容。

### 9.4 目标

目标用于为未来留出资金，和实际支出的正式专项分离。

当前流程：

- 新建目标金额和目标日期。
- 根据剩余金额和剩余月份计算建议月投入。
- 手动确认目标投入。
- 查看、编辑和撤销投入记录。
- 暂停、完成或重新启用目标。

当前问题：

- 目标日期允许选择过去日期，随后仍以至少 1 个月计算建议投入，可能产生误导。
- 新建和编辑表单发生修改后，关闭前会确认是否放弃。

### 9.5 月结

月结按所选月份汇总：

- 可支配金额。
- 支出。
- 剩余金额。
- 日常预算执行。
- 目标投入。
- 本月观察。
- 一个结论。
- 下个月一件事。

当前规则：

- 未来月份不能提前填写。
- 上月行动会出现在本月，并可标记完成。
- 正式月结内容只在点击保存后写入规划数据；输入过程另行保存为本地草稿。

当前状态：

- 月结输入实时写入带版本号的本地草稿，切换页面、月份或重新打开应用后可以恢复。
- 正式保存月结后，对应草稿自动清除；草稿不进入完整备份。

### 9.6 计划页共性问题

计划页顶部月份控制只在“预算”和“月结”显示；“专项”和“目标”作为跨月对象不显示月份控件。

## 10．数据管理与家庭成员

### 10.1 数据管理

当前功能：

- 显示本地保存和备份状态。
- 导入并预览完整备份 v5。
- 导出完整备份；在顶部操作行中位于右侧，优先照顾更高频的右手单手操作。
- 在恢复前整体校验数据和引用。
- 当前已有数据时，导入前自动触发一份本地备份下载。
- 检测旧版本时锁定写入，并提供转换后导入或开始空白 v5。
- 检测异常数据时锁定写入，并提供原始内容下载和有效记录重建。

### 10.2 家庭成员

当前功能：

- 新增和改名。
- 上移和下移。
- 设置本机默认获益方。
- 停用和恢复。
- “共同”不能停用。
- 最多启用 8 个获益方。

### 10.3 当前优点

- 导入前预览记录数、金额和主要对象数量，能在覆盖前帮助用户复核。
- 完整引用校验避免局部导入造成孤立成员、专项或目标数据。
- 数据异常时先锁定写入，再提供原始数据下载，符合本地优先产品的数据安全原则。

### 10.4 待优化问题

- “家庭成员”和备份操作混在同一按钮组中，数据管理与家庭设置的信息层级不够清楚。

本轮已经移除云朵图标。普通导出触发下载后自动关闭数据管理页面并更新备份状态，用户仍应自行确认文件真实存在；导入前保护备份必须确认保存后才继续覆盖。

## 11．反馈、错误与恢复

| 操作 | 当前反馈 | 恢复方式 |
| --- | --- | --- |
| 新增支出 | 金额成功提示 | 5 秒撤销 |
| 编辑支出 | 保存成功提示 | 无撤销，可再次编辑 |
| 删除支出 | 删除成功提示 | 12 秒撤销 |
| 确认无支出 | 成功提示 | 12 秒撤销 |
| 保存预算／目标／专项／月结 | 成功提示 | 再次编辑 |
| 导入完整备份 | 预览后确认，完成后显示记录数 | 导入前自动触发旧数据下载 |
| 数据异常 | 锁定写入并显示救援入口 | 下载原始内容后重建 |
| 导入／存储／草稿写入失败 | 页面红色错误提示，直到手动关闭 | 修正问题后重试 |
| 普通字段校验错误 | 1.8 秒 Toast | 修改后重试 |

当前策略的主要缺口是：预算等部分表单仍没有草稿恢复机制；家庭成员入口与备份入口的层级仍可继续优化。

## 12．移动端与可访问性基线

### 12.1 已确认符合

- 390px 视口下页面宽度与滚动宽度均为 390px，没有横向溢出。
- 主要导航、日期、弹窗关闭、记录操作和表单按钮以 44px 为基本触控下限。
- 手机端 `input`、`select` 和 `textarea` 强制使用至少 16px 字号，避免 iOS 聚焦放大。
- 快速记账金额和备注均为 52px 高。
- 弹窗使用 `role="dialog"` 和 `aria-modal="true"`。
- 日历日期提供包含日期和状态的可访问名称。
- Toast 使用 `aria-live="polite"`。

### 12.2 当前基线与后续优化

- 一级导航使用 `aria-current`，计划二级导航使用 `aria-selected`。
- 获益方、支出属性和无支出按钮使用 `aria-pressed`。
- 年／月折叠按钮和日历展开按钮使用 `aria-expanded`。
- 普通辅助文字已经统一提高到 `#64748b`；未来日期等不可操作状态继续通过背景和状态语义区分。
- 后续仍可减少 10～12px 的说明文字，提升弱视和户外环境下的阅读舒适度。

## 13．UX 审查结论

### 13.1 值得保留的核心设计

1. 日历作为首页第一入口，并区分“无记录”和“确认无支出”。
2. 快速记账采用显式保存，快捷记录只填充、不自动提交。
3. 支出属性、获益方、专项和备注是互相独立的维度。
4. 备注搜索与历史快捷场景联动。
5. 输入法出现时压缩表单，输入法关闭后恢复完整页面。
6. 日常预算、正式专项、财务目标和月结形成“计划—执行—复盘”闭环。
7. 对删除、无支出和新增记录提供不同长度的撤销时间。
8. 导入前完整校验，异常数据先锁定再救援。

### 13.2 本轮已经删除的伪控件

以下控件当前没有为用户提供真实能力：

1. 明细页顶部月份和“月／年”切换：视觉上像筛选器，实际不改变列表。
2. “计划—专项”顶部月份切换：专项是跨月事件，控件不改变页面内容。
3. “计划—目标”顶部月份切换：目标是长期对象，控件不改变页面内容。

建议优先删除这些伪控件，而不是为保持现状补充复杂逻辑。

### 13.3 审查问题状态

UX-01 至 UX-15 已完成；家庭成员入口与备份入口层级作为独立的后续体验观察项保留。

| 编号 | 优先级 | 问题 | 用户影响 | 建议 |
| --- | --- | --- | --- | --- |
| UX-16 | P2 | 家庭成员和备份入口层级混在一起 | 数据管理页面的任务边界不够清晰 | 后续考虑将家庭成员独立为设置入口 |

## 14．本轮实施结果与后续顺序

### 已完成：消除错误暗示

1. 删除明细页无效的月份和“月／年”控件。
2. 专项、目标页隐藏月份控件，只在预算和月结保留。
3. 历史首页使用“当月”文案，并隐藏基于今天的预测与当前专项。
4. 修正搜索占位文案和自定义日期显隐。
5. 去除云同步暗示。

这一批改动以删减和纠正为主，收益高、业务风险低。

### 已完成：优化手机主路径

1. 将一级导航改为固定底部导航。
2. 使用“首页、明细、计划”三个等宽页面入口。
3. 用右下方圆形“＋”承载记账主操作，并通过底部留白、Toast 避让和弹窗隐藏控制遮挡。
4. 数据管理继续放在页面顶部，不占用高频底部导航。
5. 为月结增加自动草稿；为复杂弹窗增加有改动时的放弃确认。

### 已完成主要部分：可信反馈和可访问性

1. 补充切换、选中和折叠状态的 ARIA 属性。
2. 系统性提高辅助文字对比度。
3. 将字段校验错误放到字段附近。
4. 调整备份完成语义，区分“已生成”和“已确认保存”。
5. 统一返回与关闭图标。
6. 导入、存储和草稿写入失败改为持久错误提示，直到用户手动关闭。
7. 增加清除本月计划的二次确认，并支持恢复未设置状态。
8. 新目标限制为今天或未来日期；编辑目标改为过去日期时增加明确确认。

## 15．后续交互变更的文档规则

以下变化必须同步更新本文档：

- 一级或二级导航调整。
- 快速记账字段、顺序、保存条件或快捷记录排序变化。
- 日历状态和点击结果变化。
- 预算、专项、目标或月结流程变化。
- 导入、导出、覆盖、数据救援或撤销规则变化。
- 手机键盘、固定区域或弹窗行为变化。
- 新增全局反馈、草稿或可访问性规范。

只修改内部纯函数、存储实现或样式细节，且不改变用户可观察行为时，无需更新本文档。
