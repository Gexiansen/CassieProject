# CassieProject Roadmap

## 当前阶段

本地优先基础、个人记账效率和完整备份能力已完成，正在建立“预算—目标—复盘”的家庭财务决策闭环；月度预算部分已完成。

## 已完成

- 完成 `docs/index.html` 的静态代码审查。
- 明确家庭共享需要独立身份、权限、同步和冲突处理，不能直接以 `localStorage` 作为共享数据源。
- 建立项目规范和路线图。
- 将金额改为整数分。
- 使用 UUID 标识账目。
- 增加数据结构版本、校验和旧数据迁移。
- 修复专项详情统计范围和日历年／月状态混用。
- 增加账目编辑、删除撤销和安全导入。
- 移除动态内联事件处理器，完善存储错误反馈。
- 将项目规范统一为 `AGENTS.md`，主账本页面统一为静态站点入口 `docs/index.html`。
- 增加 schema v2 演示账本 `test-data/account-book-demo-v2.json`，用于导入和界面效果测试。
- 增加 GitHub Pages PWA 资源：manifest、Service Worker 和本地应用图标。
- 移除 Google Fonts，`docs/` 运行时不再依赖第三方网络资源。
- 增加备份时间记录、未备份／过期提醒和导入数据摘要。
- 增加异常数据写入锁定、原始内容下载和有效记录重建流程。
- 增加最近使用分类和常用分类收藏，快捷偏好独立于账本数据保存并具备异常回退。
- 增加本地快速模板，可保存分类、金额、备注和专项并从首页直接套用。
- 增加复制最近一笔、复制明细账目和“保存并再记”。
- 增加关键词、收支、主分类、子分类、日期和金额组合筛选，并展示结果汇总。
- 增加返回本月快捷入口，筛选结果自动展开到明细。
- 增加完整备份 v1，统一导出账目、最近分类、收藏分类和快速模板。
- 增加“合并账目”“覆盖账目”和“完整恢复账目与设置”，继续兼容 schema v2 和旧数组备份。
- 为快捷设置增加独立版本和更新时间，设置变化后会触发重新备份提醒。
- 增加 decisions v1 独立规划数据，按月份保存总预算和分类预算。
- 增加“规划”页面、概览预算执行卡片、80％预警、100％超支和复制上月预算。
- 将完整备份升级到 v2，纳入规划数据并继续兼容完整备份 v1。

## 进行中

- 建立财务目标和月度复盘，使预算执行结果能够转化为下月行动。

## 待办

### 阶段三：预算—目标—复盘

- 增加财务目标、目标投入记录和建议月投入。
- 增加月度复盘、行动事项和下月跟进。

### 阶段四：个人自定义与家庭汇总基础

- 将分类和专项改为具有稳定 ID 的可管理数据。
- 只有明确需要家庭汇总时，再增加成员标识和手动文件合并规则。

## 阻塞

- 无。

## 最近验证

- `docs/index.html` JavaScript 静态语法检查通过，动态内联 `onclick` 数量为 0。
- 在 390×844 手机视口完成新增支出、含单引号专项名称、专项详情、编辑、删除撤销、年度切入日历和刷新持久化验证，浏览器控制台无报错。
- 旧版数据迁移验证通过：12.34 元迁移为 1234 分，旧 ID 转为稳定的 `legacy-*`，并写入 schema v2。
- 导入记录的日期、分类、金额和 ID 校验已通过独立函数验证；系统文件选择器未纳入自动化测试。
- 演示账本通过应用自身校验：45 笔记录全部有效，2026 年 7 月包含 18 笔收支和 2 个专项标签。
- PWA 资源语法和引用检查通过；关闭本地 HTTP 服务后，页面及本地账本仍可离线重新加载。
- 备份状态的空账本、从未备份、刚备份和超过 14 天四种分支验证通过。
- 正常、部分异常和无法解析三种本地数据验证通过；异常数据会保留原始内容并锁定写入。
- 当前目录已初始化 Git 仓库，阶段一代码已纳入 `main` 分支版本管理。
- 快捷偏好规范化验证通过：损坏 JSON 会回退默认值，无效分类、重复收藏和非法模板会被过滤。
- 在 390×844 手机视口完成常用分类收藏、模板保存与套用、保存并再记、复制最近一笔和刷新持久化验证；弹窗无横向溢出，浏览器控制台无报错。
- 筛选纯函数验证通过：关键词、收支、主／子分类、本月、上月、跨年和金额区间结果符合预期。
- 在 390×844 手机视口完成关键词与组合筛选、筛选汇总、输入校验、清空筛选和返回本月验证；页面无横向溢出，浏览器控制台无报错。
- 完整备份兼容性验证通过：完整备份 v1、schema v2 账目备份和旧数组均可解析，未知备份版本会被拒绝。
- 快捷设置部分异常时会忽略无效项；完整恢复写入失败时账目和设置均能回滚。
- 在 390×844 手机视口完成完整备份下载反馈、设置变更提醒和备份区域布局验证；页面无横向溢出，浏览器控制台无报错。
- 预算纯函数验证通过：总预算、分类预算、80％／100％阈值、跨年月份和异常规划数据均符合预期。
- 完整备份 v2、v1 兼容及账目／设置／规划三份数据的原子恢复回滚验证通过。
- 在 390×844 手机视口完成设置预算、输入校验、记录支出、分类预警、持久化和复制上月预算验证；页面无横向溢出，浏览器控制台无报错。
